{
  "name": "hashheist-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hash Heist live leaderboard — Cloudflare Worker port of ctf/server.py",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
